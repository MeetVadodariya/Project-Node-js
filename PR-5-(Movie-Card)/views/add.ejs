<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add Movie</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.0.7/css/all.css">
  <style>
    body {
      background: linear-gradient(135deg, #0d253f, #01b4e4);
      color: #fff;
      font-family: 'Poppins', sans-serif;
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      margin: 0;
    }

    form {
      background: #1c1c1c;
      padding: 30px;
      border-radius: 15px;
      box-shadow: 0px 8px 15px rgba(0, 0, 0, 0.5);
    }

    form label {
      color: #01b4e4;
    }

    form .btn-primary {
      background: #01b4e4;
      border: none;
    }

    form .btn-primary:hover {
      background: #058db9;
    }

    form .btn-outline-light:hover {
      color: #01b4e4;
      border-color: #01b4e4;
    }

    .btn {
      transition: 0.3s ease;
    }
  </style>
</head>

<body>
  <div class="container">
    <div class="row justify-content-center">
      <div class="col-lg-5 col-md-6 col-sm-8">
        <h3 class="mb-4 text-center">Add a New Movie</h3>
        <form action="/insert" method="post" enctype="multipart/form-data">
          <div class="mb-3">
            <label for="movie-name" class="form-label">Movie Title</label>
            <input type="text" id="movie-name" name="name" class="form-control" placeholder="Movie Name" required>
          </div>
          <div class="mb-3">
            <label for="movie-desc" class="form-label">Description</label>
            <textarea id="movie-desc" name="desc" rows="3" class="form-control" placeholder="A mind-bending thriller..." required></textarea>
          </div>
          <div class="mb-3">
            <label for="movie-price" class="form-label">Price (in $)</label>
            <input type="number" id="movie-price" name="price" class="form-control" placeholder="e.g. 15" required>
          </div>
          <div class="mb-3">
            <label for="movie-img" class="form-label">Movie Poster</label>
            <input type="file" id="movie-img" name="image" class="form-control" accept="image/*" required>
          </div>
          <div class="d-flex justify-content-between">
            <button type="submit" class="btn btn-primary">
              Add Movie <i class="fas fa-plus-circle"></i>
            </button>
            <a href="/views" class="btn btn-outline-light">View Collection</a>
          </div>
        </form>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
