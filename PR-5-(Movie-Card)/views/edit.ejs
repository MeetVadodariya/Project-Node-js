<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Movie</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.0.7/css/all.css">
  <style>
    body {
      background: #111827;
      color: #f9fafb;
      font-family: 'Poppins', sans-serif;
    }

    .form-container {
      background: #1f2937;
      border-radius: 12px;
      padding: 30px;
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.5);
      transition: transform 0.3s ease-in-out;
    }

    .form-container:hover {
      transform: translateY(-5px);
    }

    .form-label {
      color: #d1d5db;
    }

    .form-control {
      background: #374151;
      border: 1px solid #4b5563;
      color: #f9fafb;
    }

    .form-control:focus {
      background: #4b5563;
      border-color: #3b82f6;
      box-shadow: 0 0 5px rgba(59, 130, 246, 0.5);
    }

    .btn-primary {
      background: #2563eb;
      border: none;
    }

    .btn-primary:hover {
      background: #1d4ed8;
    }

    .img-thumbnail {
      background: #1f2937;
      border: 1px solid #374151;
      max-height: 150px;
      object-fit: contain;
    }
  </style>
</head>

<body>
  <div class="container my-5">
    <div class="row justify-content-center">
      <div class="col-md-6">
        <h3 class="mb-4 text-center">Edit Movie Details</h3>

        <form action="/update" method="post" enctype="multipart/form-data" class="form-container">
          <input type="hidden" name="editid" value="<%= singal?.id || '' %>">
          <div class="mb-3">
            <label for="movie-title" class="form-label">Movie Name</label>
            <input type="text" id="movie-title" name="name" class="form-control" placeholder="Enter movie name"
              value="<%= singal?.name || '' %>" required>
          </div>
          <div class="mb-3">
            <label for="movie-desc" class="form-label">Movie Description</label>
            <input type="text" id="movie-desc" name="desc" class="form-control" placeholder="Enter movie description"
              value="<%= singal?.desc || '' %>" required>
          </div>
          <div class="mb-3">
            <label for="movie-price" class="form-label">Price</label>
            <input type="text" id="movie-price" name="price" class="form-control" placeholder="Enter price"
              value="<%= singal?.price || '' %>" required>
          </div>
          <div class="mb-3">
            <label for="movie-img" class="form-label">Upload Image</label>
            <input type="file" id="movie-img" name="image" class="form-control">
          </div>
          <div class="mb-3 text-center">
            <img src="<%= singal?.image || '/default-image-path.jpg' %>" class="img-thumbnail" alt="Movie Image">
          </div>
          <button type="submit" class="btn btn-primary w-100">
            Submit <i class="fas fa-check"></i>
          </button>
        </form>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
